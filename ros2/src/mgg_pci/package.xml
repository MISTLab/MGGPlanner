<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>mgg_pci</name>
  <version>1.0.0</version>
  <description>
    Planner control interface: decides when to ask the planner for a path and
    publishes the result for a follower to execute.

    Written fresh rather than ported from pci_general, whose actionlib and
    PCIManager machinery is far wider than the surface actually needed.
  </description>

  <maintainer email="giovanni.beltrame@polymtl.ca">Giovanni Beltrame</maintainer>
  <license>TODO</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>mgg_msgs</depend>
  <depend>rclcpp</depend>
  <depend>nav_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>std_srvs</depend>

  <test_depend>ament_cmake_gtest</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
